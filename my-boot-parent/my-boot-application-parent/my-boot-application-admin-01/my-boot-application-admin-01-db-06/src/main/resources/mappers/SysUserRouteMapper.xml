<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.myproject.boot.application.admin.db.mapper.SysUserRouteMapper">
<select id="selectByUserId" resultType="java.lang.String">
select * from sys_route sr inner join sys_role_route srr on sr.id = srr.route_id inner join sys_user_role sur on srr.role_id = sur.role_id where sur.user_id=#{userId,jdbcType=BIGINT};
</select>
</mapper>