<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.myproject.boot.application.admin.db.mapper.SysUserRouteMapper">

<select id="selectRouteUrlByUserId" resultType="java.lang.String">
select url
from sys_user su
         inner join sys_user_role sur on su.id = sur.user_id
         inner join sys_role_route srr on sur.role_id = srr.role_id
         inner join sys_route sr on sr.id = srr.route_id
where su.id = #{userId,jdbcType=BIGINT}
</select></mapper>